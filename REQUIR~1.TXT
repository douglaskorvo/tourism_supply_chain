# Environment of the recorded run (Python 3.10.12)
# `python` is not a pip package; install Python 3.10 separately, then:
#   pip install -r requirements.txt
pandas==2.3.3
numpy==2.2.6
statsmodels==0.14.6
scipy==1.15.3
matplotlib==3.10.9
jupyter
